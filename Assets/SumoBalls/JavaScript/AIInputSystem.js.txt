var dt = sched.deltaTime();
var direction = transform.worldPosition();
direction.normalize();
direction.multiplyScalar(movement.speed() * dt * -1);
direction.add(velocity2d.velocity());
velocity2d.setVelocity(direction);
