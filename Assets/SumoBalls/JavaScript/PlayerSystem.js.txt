const arenaRadius = 7;

if (transform.worldPosition().length() > arenaRadius && player.alive()) {
   player.setAlive(false);
   entity.removeComponent(ut.Physics2D.Velocity2D);
   entity.addComponent(game.PlayerDeath);
}